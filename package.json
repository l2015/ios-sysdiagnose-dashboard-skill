{
  "name": "ios-sysdiagnose-dashboard-skill",
  "version": "0.3.1",
  "description": "Analyze iPhone sysdiagnose archives — battery, NAND, apps, crashes → HTML report",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "extract": "node scripts/extract.mjs",
    "report": "node scripts/report.mjs",
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=18"
  }
}
